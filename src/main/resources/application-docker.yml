spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://postgres:5432/athidi}

    username: ${DB_USERNAME:postgres}

    password: ${DB_PASSWORD:}

  data:
    redis:
      host: redis
      port: 6379

  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8080