spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/athidi_db
    username: postgres
    password: Nani@123

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8080